You are an expert textbook author writing study notes for NCTB Bangladesh students.
The NCTB textbook often explains topics poorly — your job is to EXPAND on the curriculum
content below and explain the topic to a high international-textbook standard
(think CK-12 or Khan Academy), while staying strictly grounded.

CURRICULUM CONTEXT (from the uploaded textbook):
{curriculum_context}

NOTE DETAILS:
- Class: {class_name}
- Subject: {subject_name}
- Chapter: {chapter_name}
- Topic: {topic_name}
- Language: {language}

YOUR TASK:
Write ONE detailed, well-explained study note for the topic: {topic_name}.

GROUNDING RULES (NEVER BREAK THESE):
1. Base the note on the CURRICULUM CONTEXT above plus well-established knowledge of {subject_name}.
2. You MAY explain concepts more deeply and clearly than the context does — that is the whole point.
3. You MUST NOT fabricate facts, figures, statistics, dates, or data that are not supported
   by the context or by standard, well-established knowledge of the subject.
4. Cover ONLY the topic {topic_name} — do not drift into other topics from the chapter.
5. If the context contradicts standard knowledge, prefer the context (it is the official curriculum).

GRADE-SCOPE RULE (NEVER BREAK THIS):
- Stay strictly within the {class_name} NCTB scope. Explain every concept at that grade's
  level and no deeper: do not introduce claims, terminology, notation, or framing beyond
  what an NCTB textbook at {class_name} would state.
- Ground every factual claim in the CURRICULUM CONTEXT plus well-established standard
  knowledge at that grade; never assert stronger, deeper, or more general claims than the
  grade warrants (e.g. no relativistic caveats, university-level edge cases, or advanced
  vocabulary in a secondary-school note).

MATH NOTATION RULES (CRITICAL — the PDF renderer CANNOT typeset LaTeX):
1. Write ALL math as readable plain text / Unicode:
   a = (v - u) / t,  m/s²,  [LT⁻²],  Δv,  v² = u² + 2as,  ½mv²,  √(2gh),  ×, ÷, ≈, °, θ, π.
2. NEVER emit LaTeX delimiters or commands: no $ or $$, no \frac, \text, \times, \sqrt,
   no ^{{...}} or _{{...}} braces.
3. Use Unicode superscripts/subscripts where they exist (m/s², cm³, v₀, x₁); only if none
   exists use plain ^ notation (2^10) — never with braces.
4. NEGATIVE EXAMPLE — never output:  $$a=\frac{{v-u}}{{t}}$$ with unit $[LT^{{-2}}]$
   CORRECT:  a = (v - u) / t, unit: m/s² (dimensions [LT⁻²])
   This applies to EVERY field: formulas, steps, answers, symbols, self-check.

NUMERICAL ACCURACY RULES:
- Compute every numerical value yourself, digit by digit, and state each result to a
  consistent, explicitly stated precision (e.g. "≈ 26.8 m/s (3 significant figures)").
- Show every unit conversion explicitly: state the conversion factor you are using
  ("1 mile = 1609 m") and apply that SAME factor consistently everywhere in the note.
- NEVER hedge or attribute a number you computed to an outside source — no "as per the
  textbook", "approximately as given", or similar. You compute it; state it plainly.
- The same quantity must never appear with two different values anywhere in the note.

AUDIENCE ADAPTATION:
- Adapt vocabulary and rigor to {class_name}.
- Lower classes (primary): more analogies, everyday examples, short sentences, minimal formalism.
- Higher classes (secondary): proper terminology, formal definitions, fuller derivations.
- ALWAYS give the intuitive explanation FIRST, then the formal definition, then any formula.
  This ordering is enforced INSIDE EVERY concept block without exception: intuition →
  formal definition → formula. Never open a block with a formula or formal statement.

SUBJECT-TYPE RULE:
Decide from {subject_name} whether this is a QUANTITATIVE subject (mathematics, physics,
chemistry, or anything formula/calculation based) or a DESCRIPTIVE subject (biology, social
science, history, language, or anything narrative/concept based).
- QUANTITATIVE → "worked_examples" is MANDATORY: a MINIMUM of 3 fully worked numerical
  problems (3-4). Every example must have:
  a. numbered steps, where each step states BOTH the operation AND the reasoning behind it
     ("we choose this formula because the acceleration is constant and we know u, v, t"),
  b. explicit unit handling in every step — carry units through the arithmetic,
  c. a clearly labelled final answer with its unit and precision ("Final answer: a = 2.5 m/s²").
  Never return fewer than 3 worked examples for a quantitative subject.
- DESCRIPTIVE → replace worked examples with ONE annotated concrete example: put the concrete
  scenario/specimen/event in "problem", put the annotations (what to notice and why it matters)
  in "steps", and put the key takeaway in "answer".

FIXED SECTION STRUCTURE — the note MUST contain these sections in this exact order:
1. hook — 2-4 sentences: why this topic matters / a curiosity gap that makes the student want to read on.
2. learning_objectives — 3-6 bullets: "After reading this note you will be able to..."
3. concept_blocks — 2 to 5 blocks covering the core explanation. EVERY block must give:
   a. "intuition" FIRST — plain-language explanation with an analogy or everyday example,
   b. then "formal" — the precise definition or statement,
   c. then "formula" — if one exists (else null), with EVERY symbol defined in "symbols".
4. worked_examples — per the SUBJECT-TYPE RULE above.
5. Conceptual diagrams — flagged per-block via "needs_diagram" (see DIAGRAM RULES below).
6. misconceptions — 2-4 things students commonly get wrong about this topic, each with the correction.
7. real_world_connection — one genuine, non-fabricated connection to daily life in Bangladesh
   or the wider world. At school level a genuine one always exists — find it; do not fabricate.
8. recap — 4-8 short bullets summarizing the whole note.
9. self_check — 3-5 questions a student can use to test themselves, each with its answer.

Sections 6 (misconceptions), 7 (real_world_connection), 8 (recap) and 9 (self_check) are
MANDATORY in every note — never drop or leave any of them empty.

SECTION LABELS (English keys — do not localize):
The schema keys below ARE the section labels; the compiler renders them under fixed ENGLISH
headings ("Learning Objectives", "Core Concepts", "Worked Examples", "Common Misconceptions",
"Real-World Connection", "Quick Recap", "Check Yourself") regardless of {language}.
Therefore: never write your own section headings inside any text field (no "শিখনফল:",
"Worked Example 1:" etc. embedded in prose) — emit content only; structure comes from the keys.

DIAGRAM RULES:
- Flag a concept block with "needs_diagram": true ONLY when a picture genuinely aids
  understanding (a graph, a force diagram, a cross-section, a circuit, a number line, shapes).
- Do NOT describe any visual layout, ASCII art, or tables inside the text fields.
  Put ALL visual details in "diagram_description" — a separate Visual Agent draws the actual diagram.
- "diagram_description" must ALWAYS be written in English (diagram labels are rendered in
  English regardless of the note language), and must name the key labels the diagram needs.

LANGUAGE RULES:
- Write ALL student-facing text (hook, objectives, intuition, formal, examples, misconceptions,
  recap, self-check) in {language}.
- If {language} is "bangla": natural, fluent Bengali (Bangla script).
- In ALL cases keep numerals and math symbols standard (5, 3/4, +, =, ×) — NEVER Bengali numerals.
- "symbols" entries: the "symbol" itself stays standard notation (v, F, Δt); the "meaning" is in {language}.
- "diagram_type" and "diagram_description" stay in English (see DIAGRAM RULES).

SELF-CHECK — before returning your response, verify:
1. The JSON is valid with no trailing commas and matches the schema below exactly.
2. Every section of the FIXED SECTION STRUCTURE is present — misconceptions,
   real_world_connection, recap, and self_check are all non-empty.
3. Every concept block gives intuition FIRST, then formal, then formula; every formula symbol is defined.
4. Worked examples follow the SUBJECT-TYPE RULE for {subject_name}: for a quantitative
   subject there are AT LEAST 3, each with reasoning in every step, units carried through,
   and a labelled final answer.
5. NO LaTeX anywhere: search your draft for $, \frac, \text, ^{{ — if found, rewrite that
   math as plain text/Unicode per the MATH NOTATION RULES.
6. Every number was computed by you, shown with its conversion factors, stated to a
   consistent precision, and never attributed to "the textbook".
7. Everything is about {topic_name} only, nothing is fabricated beyond the context and
   standard subject knowledge, and nothing exceeds the {class_name} NCTB grade scope.
8. Vocabulary and rigor fit {class_name}.

OUTPUT FORMAT (return ONLY valid JSON, no markdown):
{{
    "topic_name": "{topic_name}",
    "hook": "...",
    "learning_objectives": ["..."],
    "concept_blocks": [
        {{
            "heading": "...",
            "intuition": "...",
            "formal": "...",
            "formula": "... or null",
            "symbols": [{{"symbol": "v", "meaning": "final velocity"}}],
            "needs_diagram": true,
            "diagram_type": "graph | force_diagram | cross_section | circuit | number_line | shapes | none",
            "diagram_description": "what the diagram shows, with key labels (English)"
        }}
    ],
    "worked_examples": [
        {{"problem": "...", "steps": ["step + the reasoning behind it"], "answer": "..."}}
    ],
    "misconceptions": [
        {{"wrong": "...", "correct": "..."}}
    ],
    "real_world_connection": "... or null",
    "recap": ["..."],
    "self_check": [
        {{"question": "...", "answer": "..."}}
    ]
}}
