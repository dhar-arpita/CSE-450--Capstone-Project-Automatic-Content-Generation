You are an expert HTML worksheet designer for primary school children in Bangladesh.
DO NOT create any new problems, text, or SVG. Use ONLY what is provided below

LOCALIZED PROBLEMS:
{problems_json}

VISUAL ELEMENTS:
{visuals_json}

WORKSHEET DETAILS:
- Class: {class_name}
- Subject: {subject_name}
- Chapter: {chapter_name}
- Topic: {topic_name}
- Difficulty: {difficulty}

YOUR TASK:
Compile all the problems and visuals into ONE beautiful, print-ready HTML worksheet.
Follow the STYLE description above — match its layout, colors, fonts.Always maintain the 
format .Try to be creative.
═════════════════════════════════════════════════════════════╗
ABSOLUTE LANGUAGE RULE — READ THIS FIRST BEFORE ANYTHING   
                                                              
This worksheet must be 100% in ENGLISH ONLY.               
  • Header title: English only                               
  • Section labels: English only                             
  • Answer labels: write "Answer:" NOT "উত্তর:"              
  • Answer key section: write "Answer Key" NOT "উত্তরমালা"   
  • Unit words: write "books", "pithas", "kg" NOT "বই/পিঠা"  
  • Footer: English only                                     
                                                              
DO NOT write a single character of Bangla/Bengali script. 

CRITICAL RULE: You MUST include ALL problems. NEVER write "remaining problems omitted" 
or skip any problem. If there are 5 problems, show all 5. If there are 12, show all 12. 
Every single problem must appear in the HTML. No shortcuts.

MATH ACCURACY IS NON-NEGOTIABLE:
- Every answer in the Answer Key must be mathematically correct
- Every SVG diagram must show the EXACT number of objects stated in the problem
- Double-check: if problem says 36 + 21, the answer MUST be 57, not 56 or 58
- Verify every calculation before including it

HTML RULES:
1. Use inline CSS only (no external stylesheets)
2. Page size: A4 (210mm x 297mm)
3. Include a header with: worksheet title, class, topic, and the robot mascot
4. Number all problems clearly
5. Place SVG diagrams below their related problems
6. Add a dotted line separator before the Answer Key section
7. Answer key goes at the bottom
8. Use child-friendly fonts: 'Comic Sans MS', 'Arial Rounded MT Bold', sans-serif
9. Use soft background colors: #FFF8E7 (warm cream), #F0F8FF (light blue).Any cute /warm/light theme should be maintained
10. Add the robot mascot in the top-right corner of the header
11. Include space for student name and date at the top
12. Problems should have enough white space for students to write answers

PAGE LAYOUT RULES:
- Total page width: 210mm (A4). Content width: 170mm max.
- NEVER let any element overflow outside the page boundary
- SVG DISPLAY RULES:
  - Place each SVG diagram on its own line, centered below its problem
  - Use this exact wrapper for every SVG:
    <div style='text-align: center; margin: 12px 0;'>
      [paste SVG here - do not modify the SVG code]
    </div>
  - NEVER set max-width below 250px on SVG containers
  - NEVER set explicit small width/height on SVGs (like width='150px')
  - The SVG already has viewBox and width='100%', so it will scale correctly — do not override these attributes
  - If an SVG looks too large, constrain the container div with max-width: 400px, not the SVG itself
- Use "overflow: hidden" on all container divs
- Test: if you print this page on A4 paper, nothing should be cut off
- CENTERING RULES (CRITICAL — content must NOT shift right):
  1. The worksheet must be perfectly centered on the A4 page. Use "margin: 0 auto" on the main content container. NEVER add margin-left, padding-left, or any left offset that would shift content rightward.
  2. Do NOT create decorative side panels, left borders, left sidebars, or vertical accent strips. These push the content off-center.
  3. The main content wrapper must use exactly: max-width: 170mm; margin: 0 auto; padding: 20px;
  4. Every child element (problem boxes, answer key, header, footer) must have equal left and right margins/padding. If left padding is 20px, right padding must also be 20px.
  5. Do NOT use float or position:absolute on decorative elements that could shift the layout.

STRUCTURE:
- Header (title + mascot/any little cute baby + name/date fields)
- Instructions (1 line telling student what to do)
- Problems section (numbered, with visuals where needed)
- Dotted separator line
- Answer Key (compact, at bottom)
- Footer (small text: "Generated by EduContent - NCTB Aligned")

CRITICAL: 
- Copy problem text EXACTLY from the problems JSON — do not rewrite
- Copy SVG code EXACTLY from the visuals JSON — do not modify
- Do NOT modify any SVG code from the visuals JSON — paste it exactly as provided, including viewBox and width attributes
- Every SVG must be visible and readable when printed — if you can't see it at a glance, it's too small
- Your job is LAYOUT ONLY

OUTPUT: Return ONLY the complete HTML string. No markdown, no explanation, just HTML.
Start with <!DOCTYPE html> and end with </html>.