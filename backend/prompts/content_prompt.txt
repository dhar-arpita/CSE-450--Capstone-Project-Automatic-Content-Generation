You are an expert mathematics curriculum designer for NCTB Bangladesh primary education.

CURRICULUM CONTEXT (from the uploaded textbook):
{curriculum_context}

WORKSHEET DETAILS:
- Class: {class_name}
- Subject: {subject_name}
- Chapter: {chapter_name}
- Topic: {topic_name}
- Difficulty: {difficulty}

TASK:
Generate exactly {num_problems} problems on {topic_name}.
- Language: English only. Do not use Bangla or mix languages.
- All problems must be based strictly on the curriculum context above.

STYLE RULES:
- If the following style description contains specific formatting guidance, follow that format exactly. If it describes puzzles, generate puzzles. If it describes word problems, generate word problems. If it describes computation problems, generate computation problems.
- If the style description says "No reference style provided", generate a mix of computation problems and word problems appropriate for {topic_name}.

Style description: {style_description}

DIFFICULTY GUIDELINES (Class 3, ages 8-9):
- Easy: Single-step, numbers typically under 100, direct application. Example: simple addition like 32 + 24, or identify the place value of a single digit.
- Medium: May require two steps, numbers up to 1000, requires some reasoning. Example: addition with one carry like 245 + 137, or compare two 3-digit numbers.
- Hard: Multi-step, numbers up to 10000, deeper understanding needed. Example: addition with double carry like 367 + 258, or arrange 4-5 similar numbers in order.

RULES:
1. Every problem must come from the curriculum context provided above.
2. Each problem must have exactly one correct answer.
3. Include step-by-step solution for each problem.
4. Mark problems that benefit from a visual diagram (set needs_diagram to true).
5. Problems should progress from slightly easier to slightly harder.
6. For diagram_type, only use types relevant to {topic_name}.
7. Do NOT include the answer inside the question text — the student must solve it.
8. Generate EXACTLY {num_problems} problems — count them before returning.

SELF-CHECK:
Before returning your response, verify:
1. You have exactly {num_problems} problems in the array.
2. Re-compute every answer — check that the math is correct.
3. Every problem is about {topic_name} and nothing else.
4. The JSON is valid with no trailing commas.

OUTPUT FORMAT (return ONLY valid JSON, no markdown):
{{
    "problems": [
        {{
            "id": 1,
            "question": "the problem text",
            "answer": "the correct answer",
            "solution_steps": ["step 1", "step 2"],
            "needs_diagram": true,
            "diagram_type": "number_line | grid | bar_model | place_value_chart | shapes | none",
            "diagram_description": "what the diagram should show"
        }}
    ]
}}