fastapi==0.135.1
uvicorn==0.41.0
SQLAlchemy==2.0.48
psycopg2-binary==2.9.11
pydantic==2.12.5
python-dotenv==1.2.2
qdrant-client==1.17.0
pypdf==6.8.0
python-multipart==0.0.22
email-validator==2.3.0
google-genai
pdfplumber
pdf2image
weasyprint
mistralai
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
cryptography==41.0.7

# Step 1.3 (DEPLOYMENT_PLAN.md) — Celery worker foundation.
# gevent/psycogreen (originally added here) were removed in Step 2.1: Celery's
# soft_time_limit/time_limit are silently unenforced under gevent (confirmed
# against Celery's own docs and reproduced live), so the worker pool switched
# to prefork (Celery's default) — which needs no monkey-patching shim at all.
celery[redis]==5.6.3
flower==2.1.0